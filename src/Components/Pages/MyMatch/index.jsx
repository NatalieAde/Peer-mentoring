import React from 'react';
import { Typography } from '@material-ui/core';

export default function MyMatchPage() {
    return (
        <React.Fragment>
            <Typography>MY MATCH</Typography>
        </React.Fragment>
    )
}